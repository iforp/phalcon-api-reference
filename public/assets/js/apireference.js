$(function(){	// Enable classes tree collapsing/expanding	$('#tree > UL').collapsedList();			// Enable tabs switching	$('.tabs .tab-link').on('click', function()	{		var tab    = $(this);		var href   = $(this).attr('href');		var pannel = $(this).parents('.tabs');		if(href[0] != '#')		{			return;		}		pannel.find('.tab-link.active').removeClass('active');		$(this).addClass('active');		$('div[id^='+pannel.data('for')+']').hide();		$('#'+pannel.data('for')+'-'+href.substr(1)).show();		return false;	});});